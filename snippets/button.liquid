{%- doc -%}
  Intended for use in a block similar to the button block.

  @param {string} link - link to render
  @param {object} [block] - The block

  @example
  {% render 'button', link: '/collections/all' %}
{%- enddoc -%}

{% assign block_settings = block.settings %}
{% render 'button-custom-styles', block_settings: block_settings, block_id: block.id %}

<a
  {% if link == blank %}
    role="link"
    aria-disabled="true"
  {% else %}
    href="{{ link }}"
  {% endif %}
  class="
    size-style
    {{ block_settings.style_class }}
    {{ block_settings.style_class }}--{{ block.id }}
    css_override--{{ block.id }}
  "
  style="{% render 'size-style', settings: block_settings %}"
  {%- if block_settings.open_in_new_tab -%}
    target="_blank"
    rel="noopener noreferrer"
  {%- endif -%}
  {{ block.shopify_attributes }}
>
  {{ block_settings.label }}
  {% if block_settings.render_arrow %}
    <svg width="14" height="12" viewBox="0 0 14 12" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M7.42756 11.4546L6.44318 10.483L10.4957 6.43044H0V5.02419H10.4957L6.44318 0.98442L7.42756 4.45843e-05L13.1548 5.72732L7.42756 11.4546Z" fill="white"/>
    </svg>
  {% endif %}
</a>

{% style %}
  .{{ block_settings.style_class }}--{{ block.id }} {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-content: center;
    gap: 8px;
  }

  .{{ block_settings.style_class }}--{{ block.id }} svg {
    width: 18px !important;
    height: 13px;
    margin-top: 5px;
  }

  .css_override--{{ block.id }} {
    {{ block_settings.desktop_css_override }}
  }

  @media screen and (max-width: 768px) {
    .css_override--{{ block.id }} {
      {{ block_settings.mobile_css_override }}
    }

    .{{ block_settings.style_class }}--{{ block.id }} svg {
      width: 12px !important;
      height: 15px;
      margin-top: 4px;
    }
  }
{% endstyle %}
